# Awesome Prisma Project
        
Steps to run this project:

1. Run `npm i` command
2. Setup database settings inside `.env` file
3. Run `prisma init` command
3. Run `prisma deploy` command
3. Run `npm run start` command